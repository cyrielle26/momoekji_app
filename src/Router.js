import { HashRouter, Routes, Route } from "react-router-dom";

const Router = () => {
  return (
    <HashRouter>
      <Routes>
        <Route/>
      </Routes>
    </HashRouter>
  );
}

export default Router;